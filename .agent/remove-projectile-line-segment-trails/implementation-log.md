# Implementation Log

## Status
Code/asset/doc changes complete. Two user-only steps remain (prefab reserialize + Unity test runs) before the plan is fully closed out — see Blockers.

## Task Progress

| Task | Status | Notes |
|---|---|---|
| 001-remove-authoring-and-snapshot-path.md | Complete | |
| 002-remove-ecs-emission-path.md | Complete | |
| 003-clean-assets-and-graph-fixture.md | Complete (partial-agent) | Asset deletion + test fixture done by agent; prefab field cleanup handed to user as editor step, not hand-edited YAML |
| 004-update-tests-and-docs.md | Complete (agent portion) | Doc/test edits done; automated test runs are a user step |

## Completed Tasks
- 001-remove-authoring-and-snapshot-path.md: Removed `trailEffect`/`trailEffectShape`/`trailWidth`/`trailStepDistance` (+ properties, unused `UnityEngine.VFX`/`PlayGround.System.Combat.Vfx` usings) from `BasicAttackPrefab.cs`; removed `TrailVfxId`/`TrailWidth`/`TrailStepDistance` from `RuntimeProjectileDefinition.cs`; removed `RegisterProjectileVfx` method + call site and trail fields from `BuildProjectileTemplate` in `SkillDriver.cs`; removed 3 trail fields from `ProjectileSpawnCommand` in `ProjectileSpawnPipeline.cs`; removed `ValidateProjectileDefinition` method + call site from `SkillLoadoutValidator.cs`; removed `ProjectileVisualWarning` enum member from `SkillValidationWarning.cs` (confirmed via grep no other producer/consumer existed).
  - Validation: repo-wide grep for `TrailEffect|TrailEffectShape|TrailVfxId|TrailWidth|TrailStepDistance` (+ lowercase field variants) across `*.cs` returns zero hits outside `ProjectileDiscreteSpawnApplySystem.cs` (3 lines), which is task 002's explicit scope per the plan's declared dependency. No compiler available in this environment (no `.sln`); validation is grep-based per Step 9's allowance.

- 002-remove-ecs-emission-path.md: Removed `ProjectileTrailVfxComponent` struct + lifecycle comment from `ProjectileEcsComponents.cs` (kept `Unity.Mathematics` import — still used by `ProjectileContinuousStepComponent`). Removed the archetype entry, component-type handle, chunk array access, and `WriteCommon` parameter/argument from both `ProjectileDiscreteSpawnApplySystem.cs` and `ProjectileContinuousSpawnApplySystem.cs` (shared `ProjectileSpawnApplyUtility.WriteCommon` signature updated once, used by both). Stripped `ProjectileMovementSystem.cs` back to pure position-integration + bounds-refresh: removed `CombatAoeVfxDispatchSingleton` resolution, `LineSegmentVfxPending` field/producer-handle merge, `ProjectileTrailVfxComponent` from the query, and the distance-gate/enqueue/`LastEmitPosition` logic; removed now-unused `PlayGround.System.Combat.Vfx`, `Unity.Collections`, `Unity.Jobs`, `Unity.Mathematics` usings.
  - Validation: repo-wide grep for `ProjectileTrailVfxComponent|TrailVfxHandle|trailVfx` across `Assets/Scripts/` returns zero hits. Confirmed `TargetedResolveSystem.cs` remains the only remaining producer touching `PendingLineSegmentSpawns`/`EnqueueLineSegment`/`LineSegmentVfxEvent` (alongside shared infra in `CombatAoeVfxDispatchSystem.cs`, `VfxDataShapes.cs`, `VfxEmit.cs`). No compiler available; validation is grep-based.

- 003-clean-assets-and-graph-fixture.md: `git rm` deleted `MagicBoltTrail.vfx`/`.meta`, `FireArrowTrail.vfx`/`.meta`, `magic-bolt-2-trail.png`/`.meta`, `fire-arrow-trail.png`/`.meta`, `fire-arrow-trail-v2.png`/`.meta`. Confirmed via GUID grep before deleting: `MagicBoltTrail.vfx` (guid b82bfc0e...) referenced only by `MagicBolt.prefab`/`MagicBolt2.prefab`; `FireArrowTrail.vfx` (guid fa4db394...) referenced only by `FireArrow.prefab`; textures referenced exclusively by their respective `.vfx` (v2 texture had zero references) — matches plan's stated exclusivity exactly. Updated `AgentVfxReadCompatibilityTests.cs` `GraphPath` constant to `Assets/Vfx/LineSeg/PlagueLink.vfx` (confirmed guid 4d01f0f1... still bound to `Plague.prefab`'s `linkEffect`); left all read-only bridge assertions unchanged.
  - **Deviation from literal task text, per [[editor-steps-are-user-steps]]:** did NOT hand-edit `MagicBolt.prefab`/`MagicBolt2.prefab`/`FireArrow.prefab` YAML to strip the now-orphaned `trailEffect`/`trailEffectShape`/`trailWidth`/`trailStepDistance` entries. Since task 001 already removed those fields from the `BasicAttackPrefab` class, the orphaned entries are harmless (Unity's serializer silently drops unrecognized keys) but only disappear from the YAML once Unity reserializes the prefab. Reported as a user editor step below rather than agent-authored YAML surgery.

- 004-update-tests-and-docs.md: Removed `ProjectileTrailVfxComponent` from the manual archetypes in `CombatPoolCleanupSystemTests.cs` (EditMode, 2 occurrences), `CombatPoolCleanupSystemTests.cs` (PlayMode, 1) + its now-dead `CombatAoeVfxDispatchSystem` creation in `TickMovement` + now-unused `PlayGround.System.Combat.Vfx` using, `ProjectileContinuousSimulationTests.cs` (1, left its VFX using/dispatcher intact — that harness still runs AOE producers needing the queue owner), `ProjectileSpawnPipelineTests.cs` (2, same reasoning, VFX using left intact). Updated docs: `projectile-system.md` (removed trail from component list, archetype list, movement-system summary, frame-order step 5, batched-rendering paragraph, and the `BasicAttackPrefab` authoring bullet), `skill-system.md` (removed the trail-slot/registration paragraph, kept the AOE/targeted VFX-slot vs. sound-slot sentence), `vfx-system.md` (removed `ProjectileMovementSystem` from emitters/LineSegment-producers/authoring sections; LineSegment section now names `TargetedResolveSystem` as sole current producer), `vfx-requests.md` (removed the projectile-emission paragraph; names `TargetedResolveSystem` as the only `LineSegment` producer).
  - Validation: repo-wide grep (`Assets/`, `Docs/`) for `trail` (case-insensitive) returns zero hits in any `.cs` file and zero unrelated-doc hits (the 3 remaining `Docs/` hits — `blog-coding-agents-and-ecs.md`, `targeted-system.md`, `style-reference.md` — are pre-existing and unrelated to the projectile trail system, left untouched). The only remaining `trail`-matching files anywhere under `Assets/` are the 3 prefabs awaiting the user's reserialize step below. `LineSegment` symbol search confirms it remains only in shared VFX infra (`CombatAoeVfxDispatchSystem.cs`, `VfxDataShapes.cs`, `VfxEmit.cs`) and the targeted feature (`TargetedResolveSystem.cs`) — no projectile file references it anymore.
  - Not run: EditMode/PlayMode test suites and the manual editor scenarios listed in the task's "Validation Requested From User" section. Agents do not run Unity; this is unchanged from the plan's original expectation.

## Blockers
- None for agent-executable work — all four tasks' code/asset/doc scope is done.
- Two steps require the user in the Unity Editor before this plan can be marked fully complete (see chat summary for exact steps): (1) reserialize `MagicBolt.prefab`, `MagicBolt2.prefab`, `FireArrow.prefab` so Unity drops the orphaned `trailEffect`/`trailEffectShape`/`trailWidth`/`trailStepDistance` YAML entries now that the fields no longer exist on `BasicAttackPrefab`; (2) run the EditMode/PlayMode suites and manual scenario listed in `004-update-tests-and-docs.md` and export the XML.

## Validation Summary
- No `.sln` present in repo; agent cannot compile-check. All validation across all 4 tasks was grep/search-based (confirming zero remaining symbol references, confirming GUID exclusivity before deleting assets, confirming shared LineSegment infra untouched). No structural/behavioral code paths were left partially migrated between tasks.
