spawner spawns increasingly tankier mobs with a defined curve.

projectile spawn expansion system/job isn't multi threaded.
basic projetile spawn job is also not multi threaded

huge amount of free on main thread during projectile spawn expansion system and basic projectile spawn apply system.

combat apply finalize system has a lot of main thread work